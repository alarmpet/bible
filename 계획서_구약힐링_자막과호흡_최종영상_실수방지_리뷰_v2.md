# 자막과 호흡 최종영상 실수방지 리뷰 v2

작성일: 2026-08-12

## 검토 결론

기존 리뷰의 방향은 대체로 타당하지만, 일부 수치와 구현 완료 주장이 코드와 일치하지 않았다. 이 문서는 코드·설정·최근 실제 출력물을 대조해 타당한 규칙만 남긴 보완판이다.

## 검증된 문제

### 1. 긴 자막이 화면을 덮는 문제 — 타당

`healing_caption_policy.json`에는 `maxLines: 2`, `maxDisplayCharacters: 24`가 정의되어 있다. `caption_split_hermes.py`도 긴 텍스트를 분할할 수 있다. 그러나 최근 수동 프리뷰 렌더러들은 ASS 이벤트를 직접 만들며 긴 장면 전체 텍스트를 하나의 이벤트로 넣었다. 이 경로에서는 정책의 2줄 제한이 자동 적용되지 않는다.

결론: 정책 자체보다 수동 ASS 생성 경로가 원인이다. 앞으로 정책 기반 cue 생성 경로만 허용한다.

### 2. 자막 크기 불일치 — 타당

정책 파일은 1080p 기준 본문 96px, 성경 100px을 정의한다. 반면 수동 렌더 스크립트에는 42px, 48px, 108px이 각각 존재한다. 결과 영상마다 자막 크기가 달라진 원인이 확인된다.

결론: 자막 스타일은 한 곳에서만 생성해야 한다. 현재 체감상 작다는 요구를 반영하되 108px을 무조건 확정하지 않고, 96/100과 108/112를 실제 1분 샘플로 비교한 뒤 잠근다.

### 3. 성경 표제·셀라 노출 — 타당

최근 `scenes.json`의 성경 segment에 `(다윗의 시...)`, `(셀라)`, 악기 지시가 포함된 사례가 실제 확인됐다. `scripture_tts_prep.py`에는 제거 함수가 있지만, `tts_multi_voice.py`는 segment text를 직접 합성하므로 전처리 통과를 강제하지 않는다.

결론: `text_source`, `text_tts`, `text_caption`을 분리하고 남성 TTS·성경 자막은 정제 필드만 사용한다.

### 4. 성경 낭독의 무호흡 — 타당하나 원인은 복합적

성경 원문에 문장부호가 부족하면 TTS 입력에 문장 경계가 부족해진다. `silence_duration`은 segment 사이 간격이며, 한 segment 내부의 종결어 뒤 쉼을 보장하지 않는다.

결론: `할지어다`, `하리니`, `이로다`, `이니라`, `하시니라`, `되리라` 등의 종결 표현 뒤에 TTS 전용 마침표를 삽입하고, 긴 segment를 의미 단위로 분할한다. 화면 자막 원문에는 이 마침표를 임의로 넣지 않는다.

## 타당하지 않거나 수정이 필요한 기존 주장

### 1. “종결어 뒤 350~550ms가 SuperTonic의 보장된 pause” — 부정확

현재 엔진 호출은 `silence_duration`과 텍스트 문장부호에 의존하며, 종결어별 정확한 밀리초 pause를 공식적으로 보장하지 않는다. 따라서 350~550ms는 목표 후보값일 뿐 검증된 사실이 아니다.

수정 규칙: 종결어 정규화 후 실제 WAV waveform·scene duration을 측정해 250~700ms 범위에서 A/B 테스트하고, 청취 결과를 기준으로 잠근다.

### 2. “2초 무호흡이면 무조건 실패” — 기준 근거 부족

2초가 항상 부자연스러운 것은 아니다. 기도·묵상 구간에서는 의도된 침묵일 수 있다.

수정 규칙: 일반 성경 문장 내부의 무호흡과 장면 사이 의도된 침묵을 구분한다. 일반 문장 내부 2초 이상 무호흡은 경고, 장면·기도 구간은 별도 허용 정책으로 관리한다.

### 3. “24자는 항상 안전” — 해상도·글꼴에 따라 달라짐

24자는 현재 정책의 hard limit일 뿐 모든 글꼴·폰트 크기에서 안전한 시각 기준은 아니다. 한글 폭, 외곽선, 화면 안전영역에 따라 실제 줄 길이가 달라진다.

수정 규칙: 1920×1080 기준 한 줄 18~20자를 목표로 하고 24자를 hard fail 기준으로 유지한다. 2줄을 넘으면 글자를 축소하지 않고 cue를 분할한다.

## 최종 확정 규칙

### 자막

- 최대 2줄
- 권장 한 줄 18~20자
- 24자 초과 hard fail
- 최대 2줄 초과 시 폰트 축소 금지, 문장 분할
- `build_cues_from_manifest.py` → `build_ass_from_cues.py` 단일 경로 사용
- 수동 ASS 헤더 생성 금지
- 1920×1080 기준 스타일 하나만 사용

### 성경 TTS

- 화면 원문과 TTS 입력 분리
- 표제, `(셀라)`, 악기·편집 지시 제거
- 느낌표·반복 물음표·감정 표현 태그 제거
- 종결어 뒤 TTS 마침표 삽입
- 긴 절을 의미 단위로 분할
- 여성 나레이션과 남성 성경 낭독 두 화자만 허용

### 음성 설정 검증

현재 고정값은 문서상 `M4 / speed 0.72 / total_step 24 / silence 0.65 / pitch -8%`이지만, 공식 Supertonic 가이드는 `total_steps` 5~12를 예시·권장 범위로 제시한다. 따라서 24를 영구 확정하지 않고 8·10·12 후보를 동일 대본으로 비교한다.

또한 `audio_filter`가 config에 기록되어도 `tts_multi_voice.py`가 자동 적용하지 않는 문제가 있으므로, 후처리 완료 여부를 manifest에서 검사해야 한다.

## 최종 QA 게이트

1. 3줄 이상 자막 0개
2. 24자 초과 cue 0개
3. 표제·셀라·악기 지시문 노출 0개
4. 남성 성경 TTS 입력에 느낌표·표현 태그 0개
5. 여성 나레이션·남성 성경 낭독 외 화자 0개
6. 종결어 뒤 pause 또는 문장 경계 존재
7. ASS 스타일이 정책 경로에서 생성됨
8. 실제 1920×1080 프레임에서 자막 안전영역 확인
9. scene manifest와 자막 timeline drift 150ms 이하
10. 10초·1분·초반 3분 샘플을 각각 시청 확인

## manual.md 반영용 운영 규칙

최종 영상 생성 전 반드시 이 리뷰 v2와 `manual.md`, `research.md`를 함께 읽는다. 수동 프리뷰 스크립트로 ASS를 직접 만들지 않는다. 자막·TTS·렌더 중 하나라도 위 QA 게이트를 통과하지 못하면 최종 파일로 이름을 바꾸지 않는다.
