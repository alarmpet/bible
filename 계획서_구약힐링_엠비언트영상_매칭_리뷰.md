# 구약 힐링 엠비언트 영상 매칭 계획 리뷰

> 리뷰일: 2026-08-11  
> 대상: `계획서_구약힐링_엠비언트영상_매칭.md`  
> 확인 범위: `bible_healing/` 코드·설정·ep01 산출물·README/HANDOFF·기존 파이프라인 문서  
> 참고자료 상태: 프로젝트 전체에서 `research.md`를 찾지 못함. 따라서 연구 근거에 대한 검증은 보류하고, 아래 판단은 저장소의 실제 구현과 산출물에 근거함.

## 1. 결론

방향은 타당하다. 구약 힐링·수면형 롱폼에서 씬마다 이미지를 바꾸는 현재 구조보다, 감정·테마 블록 단위의 저밀도 플레이트가 일관성과 제작비 측면에서 적합하다.

다만 현재 계획은 설계 문서와 구현 문서 사이에 중요한 계약이 비어 있다. 특히 계획서가 제안한 `plate_timeline.json`, `build_plate_timeline.py`, `assign_plates_to_scenes.py`, `render_ambient_v1.py`는 아직 존재하지 않으며, 실제 `render_healing_v3.py`는 씬별 `scene_N_flow.jpg`를 입력으로 받아 씬별 영상을 만든 뒤 concat한다. 따라서 “플레이트를 씬에 복사/재사용하면 기존 v3를 재사용할 수 있다”는 설명만으로는 충분하지 않다.

권장 방향은 다음과 같다.

1. TTS와 오디오 lock을 최종 진실의 원천으로 유지한다.
2. `unit` 경계를 우선 보존하되, 플레이트는 오디오 시간 구간으로 확정한다.
3. 플레이트를 먼저 만들고, 각 씬에 `plate_id`를 매핑하는 결정적 manifest를 만든다.
4. 기존 v3 렌더러는 당장 교체하지 말고, 매핑된 동일 이미지를 씬별 입력으로 공급하는 호환 경로부터 구현한다.
5. 10분 규칙은 품질 목표이지 엄격한 분할 규칙으로 사용하지 않는다.

## 2. 확인된 사실과 계획의 불일치

### 2.1 `research.md`가 없음

프로젝트 루트와 하위 디렉터리를 검색했지만 `research.md`가 발견되지 않았다. 현재 계획서의 “수면/명상 채널 관행과 유사”라는 판단은 출처·측정 기준이 없다. 리뷰 문서에서는 이 주장을 사실로 확정하지 않고 가설로 취급해야 한다.

필요한 보완:

- 연구 파일을 추가하거나, 계획서에 조사 출처·표본·판단 기준을 기록한다.
- 조사 전까지는 `10분당 1장`을 UX 가설로 표기한다.
- 실제 성능 판단은 완주율, 이탈 구간, 댓글의 지루함/방해 피드백, 모바일 시청 품질로 검증한다.

### 2.2 현재 구현은 씬 단위 이미지 계약임

`bible_healing/scripts/render_healing_v3.py`의 `render_scene()`는 다음 파일을 직접 요구한다.

```text
scene_{order}_flow.jpg
scene_{order}.wav
```

이후 `concat_and_burn()`이 씬 영상을 concat하고 ASS를 전체 타임라인에 burn-in한다. 현재 `scenes.json`도 각 씬에 `image: scene_N_flow.jpg`를 저장한다. 즉, 플레이트 개념은 코드에 아직 존재하지 않는다.

따라서 다음 문장은 구현 조건이 추가되어야 한다.

> “`scene_N_flow.jpg`를 플레이트 에셋으로 복사/심볼릭 링크해 기존 v3 렌더를 재사용한다.”

최소 계약은 아래처럼 명시해야 한다.

```json
{
  "schema_version": "1.0",
  "episode_id": "ep01_anxious_night",
  "audio_manifest_sha256": "...",
  "plates": [
    {
      "id": "P01",
      "start_sec": 0.0,
      "end_sec": 612.4,
      "unit_ids": ["opening", "u01", "u02"],
      "asset": "assets/generated/ep01/plates/P01.jpg",
      "motion": {"type": "still", "zoom_start": 1.0, "zoom_end": 1.0}
    }
  ],
  "scene_assignments": [
    {"order": 1, "plate_id": "P01", "start_sec": 0.0, "end_sec": 18.2}
  ]
}
```

핵심은 플레이트 구간과 씬 매핑을 하나의 버전 관리 가능한 산출물로 남기는 것이다. 이미지 파일을 단순 복사하면 나중에 어느 씬이 어느 플레이트를 썼는지 추적하기 어렵다.

### 2.3 분량과 플레이트 수 기준이 서로 충돌함

저장소에는 ep01 순수 낭독이 약 85분 47초, 쉼을 포함한 권장본이 100분이라는 기록이 있다. 반면 계획서의 예시는 100분을 중심으로 작성되어 있고, 실제 구현 시점에는 TTS와 padding이 바뀔 수 있다.

따라서 플레이트 수는 다음처럼 최종 locked duration으로 계산해야 한다.

```text
target_count = clamp(ceil(locked_duration_sec / 600), 8, 12)
```

단, `clamp`는 8~12개를 강제하므로 80분 미만 콘텐츠에 부자연스러운 긴 플레이트를 만들 수 있다. 더 안전한 운영 규칙은 다음이다.

- 60분 미만: 6~8개
- 60~120분: `ceil(duration / 600)`을 기본값으로 하고 8~12개 범위에서 조정
- 120분 초과: 장르·완주율 데이터를 보고 분할

계획서의 “각 플레이트 연속 체류 8–12분”과 품질 게이트의 “단일 플레이트 ≥ 6분”은 함께 존재할 수 있지만, 최소·목표·상한을 구분해 써야 한다.

### 2.4 `scene_audio_manifest`와 `scenes.json`의 역할이 불명확함

계획서는 두 파일을 함께 사용한다고 적었지만 어느 파일의 시간과 순서를 authoritative로 삼는지 정의하지 않았다. 실제 파이프라인에서는 TTS 후 `scene_audio_manifest.json`을 lock하고, ASS는 manifest에서 cue를 만드는 구조다.

권장 순서:

```text
TTS 완료
  → scene_audio_manifest lock
  → scene별 start/end 계산
  → unit별 시간 집계
  → plate_timeline 생성
  → scene_assignments 생성
  → 이미지 준비
  → 기존 v3 렌더
  → QA
```

`scenes.json`은 텍스트·unit·speaker 메타데이터를 제공하고, 시간은 lock된 오디오 manifest에서만 읽는다. 둘의 시간이 다르면 실패시키는 검사를 추가해야 한다.

### 2.5 Ken Burns 단위가 부정확함

계획서에는 `4%/분`, `5%/분 이하`, `1.00→1.04 / 8–12분`이 함께 적혀 있다. `4%/분`을 문자 그대로 적용하면 10분 동안 40% 줌이 되어 “극슬로우”가 아니다. 의도는 “플레이트 전체에서 총 4% 줌”으로 보인다.

권장 표기:

```text
zoom_total_per_plate: 0.04
zoom_rate: zoom_total / plate_duration_sec
```

또한 `render_healing_v3.py`에는 Ken Burns나 crossfade 구현이 보이지 않는다. 계획의 파라미터를 코드에 반영하기 전에는 QA 게이트로 선언하면 안 된다.

### 2.6 crossfade 계획이 현재 concat 구현과 맞지 않음

현재 v3는 씬 영상을 concat하며, 계획서는 플레이트 사이에 1.2–2.0초 crossfade를 요구한다. 두 영상의 접합부에 crossfade를 넣으려면 단순 concat이 아니라 영상·오디오의 overlap과 길이 보정이 필요하다. 특히 씬 단위로 같은 이미지를 반복 공급하는 호환 경로에서는 씬 경계마다 fade가 생기면 안 되고, 플레이트 경계에만 생겨야 한다.

초기 구현에서는 crossfade를 필수로 넣지 말고, 정지 이미지와 동일 이미지 교체가 검증된 뒤 플레이트 경계에만 적용하는 것이 안전하다. crossfade를 넣는다면 `plate_id` 경계에서 한 번만 생성하고, 오디오를 변형하지 않는지 확인해야 한다.

## 3. 추가로 발견된 품질·운영 리스크

### 3.1 기존 산출물의 품질 문제를 플레이트 계획이 해결하지 않음

`bible_healing/HANDOFF.md`는 구 100분본의 느린 음성, 이미지 없음, MP4 내 자막 미합성을 배포 금지 사유로 기록한다. 플레이트 계획은 비주얼 구조를 개선하지만, 다음 기존 게이트를 대체하지 않는다.

- 음성 speed/voice 기본값 고정 및 실제 `voice_map.json`과의 일치
- ASS가 최종 MP4에 burn-in 되었는지 확인
- 무음 패딩과 총 길이 검증
- 이미지 파일 존재·해상도·종횡비 검사
- 라이선스/provenance 기록

플레이트 기능은 `qa_healing_render.py`와 독립된 새 QA가 아니라 기존 QA를 통과한 뒤 추가 검사를 붙이는 방식이 좋다.

### 3.2 “말씀 구간에는 플레이트 고정” 규칙은 unit보다 강한 우선순위가 필요함

현재 scene 메타에는 `unit`, `speaker`, `ref`가 있다. 말씀 구간이 플레이트 경계에서 시작하거나 끝나면, 10분 경계를 그대로 적용할 경우 말씀 중 이미지가 바뀔 수 있다.

우선순위는 다음이 적절하다.

1. 같은 성경 낭독 구간 내부에는 플레이트 전환 금지
2. 가능하면 같은 `unit` 안에서 전환 금지
3. 그 다음에 10분 목표에 가깝게 조정
4. 6분 미만 또는 14분 초과가 되면 경고를 남기고 수동 검토

즉, 시간은 soft constraint이고 음성·말씀 연속성이 hard constraint다.

### 3.3 OCR만으로는 이미지 품질을 충분히 검증할 수 없음

“이미지 내 OCR 본문 없음”은 필요하지만 충분하지 않다. 생성 이미지의 작은 로고·십자가·사람 얼굴·불쾌한 왜곡·과도한 대비는 OCR에 잡히지 않을 수 있다.

추가 QA 항목:

- OCR 탐지 텍스트 0건
- 얼굴/사람 탐지: 의도하지 않은 경우 0건
- 검은 프레임·깨진 이미지·잘못된 비율 0건
- 평균 밝기와 대비의 급격한 변화 경고
- 연속 플레이트 간 장면·색온도 중복 또는 충돌 수동 검토
- `provenance.json`에 생성 방식, 원본, 라이선스, 모델/프롬프트 기록

### 3.4 영상 목표와 채널 목표가 분리되어 있음

“10분당 1장”은 제작 규칙이지 시청자 가치의 직접 지표가 아니다. 힐링 영상에서는 시각 자극을 줄이는 것과 지루함을 줄이는 것이 동시에 필요하다. 따라서 첫 스모크는 전체 100분보다 12–15분 구간으로 검증하는 편이 낫다.

권장 실험:

- A: 순수 정지 이미지
- B: 플레이트 전체 4% Ken Burns
- C: 10분 플레이트 + 특수 모티프에서만 4–8초 루프

동일 오디오로 비교하고, 자막 가독성·수면 방해·화면 전환 인지 정도를 확인한다.

## 4. 권장 구현 설계

### Phase 0: 계약 먼저 고정

추가할 파일:

- `bible_healing/config/ambient_plates_ep01.yaml`
- `bible_healing/schemas/plate_timeline.schema.json` 또는 프로젝트 내 동일 목적의 JSON schema
- `bible_healing/scripts/build_plate_timeline.py`
- `bible_healing/scripts/assign_plates_to_scenes.py`
- `bible_healing/scripts/qa_ambient_plates.py`

`ambient_plates_ep01.yaml`은 키워드·모티프·프롬프트·허용 모션만 보유하고, 시간은 보유하지 않는다. 시간은 lock된 오디오에서 `build_plate_timeline.py`가 생성한다.

### Phase 1: 10분 스모크

전체 Full 렌더 전에 preview job에서 다음을 검증한다.

1. 12–15분 이상의 오디오를 선택한다.
2. 1–2개의 플레이트를 생성한다.
3. 모든 scene에 `plate_id`를 매핑한다.
4. 기존 `render_healing_v3.py`로 렌더한다.
5. ASS 시간축이 유지되는지 확인한다.
6. 플레이트 경계에서 급컷·검은 프레임·오디오 단절이 없는지 확인한다.

이 단계에서는 새 렌더러를 만들지 말고, 현재 v3의 입력 계약을 이용한다. 호환 경로가 확인된 뒤에만 플레이트 단위 렌더 최적화를 고려한다.

### Phase 2: Full 플레이트 생성

플레이트 에셋은 16:9 고해상도 JPG/PNG로 저장하고, 생성 결과마다 다음을 기록한다.

```json
{
  "asset": "plates/P01.jpg",
  "sha256": "...",
  "source": "generated",
  "prompt": "...",
  "negative_prompt": "text, logo, watermark, face",
  "review": {"ocr": false, "faces": false, "approved": true}
}
```

### Phase 3: QA 게이트 개정안

| ID | 게이트 |
|---|---|
| V1 | locked audio 전체 구간이 정확히 한 번씩 플레이트에 포함됨 |
| V2 | 모든 scene에 정확히 하나의 `plate_id`가 있음 |
| V3 | 말씀 낭독 중간에 플레이트 전환이 없음 |
| V4 | 플레이트 수가 duration 목표 범위 안에 있고, 6분 미만/14분 초과를 경고함 |
| V5 | 모든 이미지가 존재하고 16:9·최소 해상도를 충족함 |
| V6 | OCR 텍스트·로고·워터마크·의도하지 않은 얼굴이 없음 |
| V7 | ASS가 최종 영상에 합성되고 첫·중간·끝 자막을 샘플 검증함 |
| V8 | 오디오 총 길이와 최종 영상 길이 차이가 허용 오차 안임 |
| V9 | 모든 에셋의 provenance와 prompt가 보존됨 |
| V10 | 수동 시청에서 플레이트 경계의 급격한 밝기·색·움직임 변화가 없음 |

## 5. 계획서에 반영해야 할 문구

다음 항목은 원문 계획서에 직접 반영하는 것이 좋다.

- `research.md`가 없으므로 “채널 관행” 표현을 가설로 낮춘다.
- `scene_audio_manifest`를 시간의 단일 기준으로 명시한다.
- `plate_timeline.json`을 입력이 아니라 lock 이후 생성되는 결정적 산출물로 정의한다.
- `scene_assignments`를 별도 manifest로 저장한다.
- `render_healing_v3.py`는 플레이트를 직접 이해하지 않으며, 초기에는 매핑된 `scene_N_flow.jpg`를 소비하는 호환 경로를 사용한다고 명시한다.
- Ken Burns는 “분당 4%”가 아니라 “플레이트 전체 최대 4%”로 수정한다.
- crossfade는 플레이트 경계에만 적용하며 초기 smoke에서는 선택 기능으로 둔다.
- 현재 Handoff의 기존 음성·자막·이미지 QA를 플레이트 QA와 함께 통과해야 한다.

## 6. 최종 판단

이 계획은 채택 가능하지만, 현재 상태로 바로 Full 구현에 들어가면 시간축·렌더 입력·플레이트 경계·자막 합성 문제가 뒤늦게 드러날 가능성이 높다. 먼저 12–15분 smoke에서 manifest 계약과 기존 v3 호환성을 검증한 후, Full 100분본으로 확장하는 순서가 적절하다.

우선순위는 다음과 같다.

1. `research.md` 보완 또는 연구 근거를 가설로 명시
2. locked audio 기반 timeline/assignment schema 확정
3. 1–2 플레이트 smoke 렌더 및 ASS/오디오 회귀 검증
4. QA 자동화
5. Full 플레이트 생성 및 본편 렌더

