# 구약 힐링 영상 품질 재작업 계획서

작성일: 2026-08-11
대상 영상: `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/final-bible-healing-ep01-hook-v3.mp4`
관련 매뉴얼: `manual.md`
관련 실행 로그: `계획서_구약힐링_첫1분훅_진성엠비언트_실행로그_20260811_render완료.md`

## 1. 사용자 검수에서 확인된 핵심 문제

1. 영상에 자막이 보이지 않는다.
2. 영상이 ambient 영상이 아니라 정지 이미지 중심의 단순 slideshow다.
3. 남성 성경 낭독이 빠르고 화난 듯 들린다.

세 문제는 서로 다른 파이프라인 단계의 문제이므로 한 번에 속도만 조정하지 않고 각각 독립적으로 검증한다.

## 2. 현재 구현과 증거

### 2.1 자막

현재 자막 파일은 존재한다.

- `hermes_jobs/full/subtitles-timed-ko.srt`
- `hermes_jobs/full/subtitles-timed-ko.ass`

하지만 최종 영상은 `scripts/render_simple_longform.py`로 생성됐다. 이 renderer는 `scene_N_flow.jpg + scene_N.wav`를 합쳐 MP4를 만들고, `ASS`를 영상에 burn-in하지 않는다. 내부에 `write_srt()`가 있지만 이는 별도 SRT 파일을 생성할 뿐 영상에 자막을 입히지 않는다.

반면 `scripts/render_healing_v3.py`의 `concat_and_burn()`은 ASS를 ffmpeg `ass` filter로 영상에 태우는 경로를 갖고 있다. 현재 최종 영상은 이 경로를 사용하지 않았다.

판정: 자막 데이터 생성 실패가 아니라, 최종 render 경로에서 burn-in 단계가 빠진 통합 문제다.

### 2.2 ambient

`plate_timeline.json`과 `scene_assignments`는 생성됐고 110 scene이 모두 plate에 배정됐다. 그러나 `render_simple_longform.py`의 `render_scene()`는 다음 파일만 사용한다.

```text
scene_N_flow.jpg
scene_N.wav
```

즉 `plate_timeline.json`, `P01.jpg`–`P10.jpg`, plate의 motion 설정을 읽지 않는다. 결과적으로 plate가 바뀌어도 영상에는 plate 정책이 반영되지 않는다.

현재 ambient assets:

```text
bible_healing/assets/generated/ep01/plates/P01.jpg ... P10.jpg
```

판정: plate 배정 데이터는 있으나 renderer가 그 데이터를 소비하지 않는 연결 단절이다. 현재 영상은 진성 ambient motion 영상이 아니다.

### 2.3 남성 목소리

현재 설정:

```text
scripture voice: M5
speed: 0.85
total_step: 16
silence_duration: 0.35
max_chunk_length: 200
```

확인 경로:

- `bible_healing/config/voice_healing.yaml`
- `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/voice_map.json`
- `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/scene_audio_manifest.json`

M5가 숫자상 0.85로 설정되어 있어도 다음 요인이 화난 듯한 인상을 만들 수 있다.

- M5 음색의 단단하고 전진하는 어택
- 낮은 speed만으로는 해결되지 않는 억양·강세 문제
- 긴 성경 문장을 200자 단위로 처리하면서 문장 끝 여유가 부족함
- scene 간 silence가 0.35초로 짧아 청자가 호흡할 공간이 적음
- scripture 전체 26 scene이 동일한 voice/style로 생성됨

현재 문제는 “속도만 빠르다”가 아니라 음색, speed, pause, prosody의 조합 문제로 정의한다.

## 3. 재작업 목표

### 자막 목표

- 모든 낭독 구간에 한국어 자막이 실제 영상 프레임에 표시된다.
- 자막은 음성 시작·종료와 일치한다.
- 한 화면 과밀을 피하고 2줄 이내, 짧은 호흡 단위로 표시한다.
- 첫 1분 훅 자막도 표시되며 `hook_phase`와 시간축이 어긋나지 않는다.

### ambient 목표

- plate timeline의 asset이 실제 영상에 사용된다.
- 동일 이미지가 단순 정지 화면으로만 보이지 않도록 느린 zoom/pan 또는 미세한 motion을 적용한다.
- plate 전환은 오디오 scene 경계와 무관하게 갑자기 튀지 않고 crossfade한다.
- P06의 과도한 체류와 P01–P05의 짧은 체류 경고를 시각적으로 재검토한다.

### 남성 음성 목표

- 성경 낭독이 명령·질책·뉴스 낭독처럼 들리지 않는다.
- 여성 narrator보다 차분하고 낮지만 위압적이지 않다.
- 문장 사이와 절 사이에 충분한 호흡이 있다.
- 첫 1분과 Psalm 4 전체에서 위화감이 없어야 한다.

## 4. 재작업 순서

### Phase 0 — 기준 보존

- 현재 문제 영상과 기존 산출물을 삭제하지 않는다.
- 새 결과는 다음처럼 버전명을 붙인다.

```text
final-ep01-hook-v4-caption-ambient-voiceB.mp4
```

- 새 음성은 기존 `scene_*.wav`와 섞지 않고 별도 batch/job에서 생성한다.

### Phase 1 — 남성 음성 A/B 테스트

최소 3개 후보를 같은 텍스트로 비교한다.

| 후보 | voice | speed | total_step | silence | 목적 |
|---|---:|---:|---:|---:|---|
| A | M5 | 0.76 | 20 | 0.48 | 기존 음색 유지, 속도·호흡 완화 |
| B | M3 | 0.76 | 20 | 0.48 | 더 부드러운 음색 후보 |
| C | M2 또는 M4 | 0.78 | 20 | 0.45 | 선명도와 안정감 비교 |

후보 텍스트:

- 첫 훅 직후 Psalm 4 시작 1개
- Psalm 46처럼 긴 본문 1개
- 짧은 구절과 긴 문장을 섞은 narrator bridge 1개

선정 기준:

- 속도보다 억양의 압박감이 낮은가
- 문장 끝이 올라가거나 공격적으로 끊기지 않는가
- 저음이 무겁더라도 위협적으로 들리지 않는가
- 30초 이상 들어도 피로하지 않은가

선정 전에는 전체 TTS를 재생성하지 않는다.

### Phase 2 — 성경 음성 재생성

- 선택한 voice/speed/silence를 `voice_healing.yaml`과 voice_map에 반영한다.
- scripture만 새로 생성하고 narrator 음성은 유지한다.
- 새 manifest를 lock한다.
- 첫 구절 시작 목표 `45–55초`가 유지되는지 확인한다.
- 기존 full duration과 새 duration 차이를 기록한다.

필수 QA:

```powershell
python scripts/qa_first_minute.py --job <new-job>
python scripts/qa_healing_script.py --episode ep01_anxious_night
```

### Phase 3 — 자막 burn-in renderer 구축

선택 방향:

1. `render_healing_v3.py`를 새 manifest/plate renderer와 통합한다.
2. 또는 `render_ambient_v4.py`를 새로 만들어 concat 후 ASS burn-in을 단일 경로로 보장한다.

필수 조건:

- 최종 MP4 생성 명령에 `ass` 또는 `subtitles` ffmpeg filter가 반드시 포함된다.
- ASS 파일 경로를 Windows에서 안전하게 escape한다.
- 자막 없는 body와 자막 포함 final을 별도 파일로 보존한다.

검증:

- 첫 60초 frame을 추출한다.
- 자막 영역에 실제 글자가 있는지 확인한다.
- 자막 파일 존재만으로 통과시키지 않는다.
- MP4에 영상 stream과 audio stream이 모두 있는지 확인한다.

### Phase 4 — 진성 ambient renderer 구축

입력:

- `scene_audio_manifest.json`
- `plate_timeline.json`
- `scene_assignments`
- `assets/generated/ep01/plates/P01.jpg`–`P10.jpg`
- 새 `scene_*.wav`
- `subtitles-timed-ko.ass`

렌더 규칙:

- 각 scene은 `scene_assignments[order].plate_id`의 asset을 사용한다.
- plate 내부에서는 느린 Ken Burns 또는 미세한 pan을 적용한다.
- plate 전환은 1.5초 crossfade를 기본값으로 한다.
- 자막은 최종 concat 단계에서 ASS burn-in한다.
- plate motion은 오디오 duration을 시간 권위로 삼는다.

현재 `render_simple_longform.py`는 plate를 읽지 않으므로 최종 production renderer로 사용하지 않는다.

### Phase 5 — 최종 통합 QA

필수 검사:

- 자막 burn-in frame QA
- ambient plate asset과 장면 매칭 QA
- 남성 voice A/B 선택 결과 재청취
- 전체 MP4 duration과 audio duration 비교
- 첫 1분 첫 구절 45–55초
- scene MP4 decode QA
- 자막 cue violation 0
- ambient QA errors 0

## 5. 완료 기준

다음 조건을 모두 만족하기 전에는 최종본으로 표시하지 않는다.

- [ ] 자막이 영상 프레임에 실제 표시됨
- [ ] 첫 1분 자막과 음성 싱크 통과
- [ ] plate asset이 실제 영상에 사용됨
- [ ] 단순 정지 이미지 slideshow가 아님
- [ ] 남성 목소리 후보 청취 승인
- [ ] full manifest와 final video duration 일치
- [ ] 전체 110 scene decode 통과
- [ ] QA report와 실행 로그 기록 완료

## 6. 다음 실행 우선순위

1. 남성 voice A/B 샘플 생성 및 청취 비교
2. 자막 burn-in smoke 60초 생성
3. plate asset 기반 ambient smoke 60초 생성
4. 세 결과를 합친 60초 통합 smoke
5. 통합 smoke 승인 후 full 재생성

