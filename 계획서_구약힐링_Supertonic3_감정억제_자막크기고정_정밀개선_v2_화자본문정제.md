# SuperTonic3 정밀 개선 계획 v2 — 화자 2종·성경 본문 정제 고정

이 문서는 다음 계획서의 보완판이다.

`계획서_구약힐링_Supertonic3_감정억제_자막크기고정_정밀개선.md`

앞으로 생성하는 모든 프리뷰·샘플·본편에 반드시 적용한다.

## 화자 규칙

허용 화자는 정확히 두 종류뿐이다.

1. `narrator_female`: 위로·안내·해설을 읽는 여자 나레이션
2. `scripture_male`: 정제된 성경 본문만 읽는 남자 성경 낭독

다음은 금지한다.

- 남자가 해설·위로 문장을 읽는 것
- 여자가 성경 본문을 읽는 것
- 제3의 화자·캐릭터·대화 파트 추가
- 한 장면 안에서 화자를 임의로 교체

## 성경 본문 정제 규칙

다음 항목은 화면 자막과 TTS 입력에서 모두 제거한다.

- `(다윗의 시)`
- `(영장으로 현악에 맞춘 노래)`
- `(셀라)` 및 `Selah`
- `아삽의 시`, `고라 자손의 시`
- `영장으로`, `현악`, `스미닛` 등 악기·예배·편집 지시
- 괄호 안 저자·장르·연주·편집 메타데이터
- 절 번호, 장 번호, 역본명, 각주, 번역자 주석

원문 전체는 보존하되 낭독·자막용 텍스트를 별도 생성한다.

```json
{
  "text_display_source": "원문 전체",
  "text_scripture_clean": "표제·셀라·지시문을 제거한 실제 성경 본문"
}
```

남성 TTS와 남성 자막은 반드시 `text_scripture_clean`만 사용한다. `text_display_source`가 TTS나 최종 자막에 들어가면 QA 실패다.

## TTS 감정 억제 규칙

- `!`, `!!`, `!?`, `❗`을 TTS 입력에서 마침표로 정규화
- 표현 태그 `<laugh>`, `<breath>`, `<sigh>` 사용 금지
- 성경 본문은 문장·절·쉼표 단위로 짧게 분할
- 표제·셀라·악기 지시 제거 후에만 SuperTonic3 호출
- 여성 나레이션과 남성 성경 본문을 별도 segment로 유지

## 자막 규칙

- 자막은 반드시 정제된 본문에서 생성
- 장면·문장 단위가 아닌 짧은 구절 단위로 분할
- 최대 2줄, 한 줄 최대 24자
- 1920×1080 기준 본문 108px, 성경 본문 112px 후보
- Malgun Gothic Bold, 고정 외곽선·그림자·위치
- 수동 ASS 헤더 생성 금지
- `build_ass_from_cues.py` 단일 경로만 사용
- 자막 크기를 글자 수에 따라 자동 축소하지 않음

## 필수 QA

1. 화자가 `narrator_female`과 `scripture_male` 두 종류뿐인가
2. 남성 성경 텍스트에 `다윗의 시`, `셀라`, `영장`, `현악`, `스미닛`, 괄호 메타데이터가 없는가
3. TTS 입력에 느낌표·표현 태그가 없는가
4. 화면 자막에도 표제·셀라·지시문이 없는가
5. 실제 TTS 입력과 자막 텍스트의 정제본 해시가 일치하는가
6. 모든 장면이 동일한 남성 음성·속도·피치 후처리를 사용하는가
7. 자막이 108/112px 기준보다 작아지지 않는가

## 적용 범위

이 규칙은 앞으로 생성되는 10초 샘플, 1분 프리뷰, 초반 3분, 전체 본편, 재렌더, 자막 재출력에 모두 적용한다. 과거 산출물은 비교용으로 보존하지만 새 결과물로 사용하지 않는다.
