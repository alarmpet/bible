# 실행 로그 추가 — 2026-08-11

기준 문서: `manual.md`, `계획서_구약힐링_첫1분훅_진성엠비언트_실행로그.md`

## 수행

- `manual.md`를 새로 작성해 다른 LLM이 동일 workspace에서 작업을 이어받을 수 있도록 했다.
- 첫 1분 smoke 통과 후 full 110 scene TTS 생성을 재시도했다.
- 전체 합성은 제한 시간 내 완료되지 않아 중단했다.
- 생성된 부분 wav 13개는 활성 full job에서 다음 백업 폴더로 이동했다.
  - `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/audio_partial_run2_backup_20260811/`
- 기존 백업과 이름이 겹친 14개 wav는 기존 폴더에 보존되어 있다.
- 활성 full job에는 현재 `scene_*.wav`와 유효한 `scene_audio_manifest.json`이 없다.

## 판단

- full TTS 미완료 상태에서 render, chapter timestamp, upload package 생성은 진행하지 않는다.
- `hook_smoke_v2`의 첫 1분 결과는 여전히 유효하다.
  - 첫 성경 구절: 48.552925초
  - QA: 통과
- 다음 재개 시 full TTS는 기존 partial 파일을 재사용하지 않고, scene 수·대본 hash 확인 후 별도 날짜 백업을 먼저 만든다.

## 다음 작업

1. full TTS를 더 긴 실행 세션 또는 scene batch 방식으로 완료한다.
2. `lock_audio_manifest.py`를 실행한다.
3. full `qa_first_minute.py` 통과를 확인한다.
4. 그 이후에만 ambient timeline과 render를 진행한다.
