# 최종 영상 실수 방지 계획 — 자막 2줄·성경 낭독 호흡

작성일: 2026-08-12

## 확인된 문제

첨부 캡처처럼 한 자막 이벤트에 성경 구절 전체가 들어가면 5줄 이상이 화면을 덮는다. 동시에 원문에 문장부호가 거의 없으면 `할지어다`, `하리니`, `이로다` 같은 종결 표현 뒤에도 음성이 쉬지 않고 이어진다.

## 필수 자막 규칙

- 한 화면 최대 2줄
- 한 줄 최대 18~20자, 1920×1080 기준 최대 24자를 절대 상한으로 사용
- 자막 이벤트 하나에 긴 절 전체를 넣지 않음
- 문장부호뿐 아니라 의미 단위로 강제 분할
- 자막 크기를 줄여 한 화면에 욱여넣지 않음
- 1080p 기준 본문 108px, 성경 본문 112px 고정
- 자막은 항상 화면 하단 안전영역에 배치
- 자막 개수·줄 수·문자 수를 렌더 전 QA

## 성경 TTS 호흡 규칙

성경 정제 단계에서 다음 종결어 뒤에 TTS용 마침표를 자동 삽입한다.

```text
할지어다, 하리니, 이로다, 이니라, 하시니라, 하였느니라,
되리라, 있으리라, 없느니라, 아멘
```

다만 화면 자막용 원문에는 임의의 마침표를 넣지 않고, `text_display_source`와 `text_tts`를 분리한다.

예:

```text
화면 원문: 너희는 떨며 범죄하지 말지어다 자리에 누워 심중에 말하고 잠잠할지어다
TTS 입력: 너희는 떨며 범죄하지 말지어다. 자리에 누워 심중에 말하고 잠잠할지어다.
```

종결어 뒤 기본 pause는 350~550ms, 절과 절 사이 pause는 550~800ms를 후보로 테스트한다. 쉼표는 180~280ms를 기본값으로 한다.

## 표제·지시문 제거

남성 성경 낭독과 성경 자막에는 다음을 넣지 않는다.

- `(다윗의 시)`
- `(영장으로 현악에 맞춘 노래)`
- `(셀라)` / `Selah`
- 저자·악기·예배·편집 지시문
- 절 번호·각주·역본명

원문 보존본에는 남기되 낭독·자막용 정제본에서는 제거한다.

## 최종 렌더 전 필수 순서

1. 원문 보존본 생성
2. TTS 정제본 생성
3. 종결어 기반 pause 삽입
4. TTS 합성
5. 실제 WAV duration lock
6. 정제된 표시 텍스트를 음성 구간에 맞춰 phrase cue 생성
7. 2줄·문자 수 QA
8. 고정 ASS 스타일 생성
9. 자막 번인
10. 10초·1분·3분 샘플을 화면으로 확인

## 절대 금지

- 장면 전체 대사를 자막 하나에 넣기
- 자동 줄바꿈만 믿고 렌더하기
- 글자가 많을 때 글꼴을 작게 줄이기
- 화면 자막과 TTS 입력을 같은 문자열로 무조건 사용하기
- TTS 전처리 없이 `scenes.json`을 바로 합성기에 전달하기
- 자막 스타일을 스크립트마다 직접 작성하기
- 음성·자막·영상 타임라인을 추정 글자 수만으로 확정하기

## QA 실패 조건

- 3줄 이상 자막 1개라도 존재
- 24자 초과 단일 라인 존재
- 화면을 가리는 자막 영역이 기준 안전영역을 초과
- 2초 이상 무호흡으로 이어지는 성경 문장
- 종결어 뒤 pause가 없는 문장
- `셀라`, 표제, 악기 지시문이 남성 TTS나 자막에 포함
- 렌더에 사용한 ASS가 정책 파일에서 생성되지 않음

## 다른 LLM 인수인계 메모

최종 영상 생성 전 반드시 이 문서와 `manual.md`, `research.md`, 현재 실행 로그를 함께 읽는다. 자막·음성·영상 중 하나라도 이 규칙과 다르면 렌더를 완료로 보고하지 않는다.
