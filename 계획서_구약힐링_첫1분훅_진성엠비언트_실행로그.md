# 구약 힐링 첫 1분 훅 — 실행 로그

기준 계획서: `계획서_구약힐링_첫1분훅_진성엠비언트.md`
조사 문서: `research.md`
최종 갱신: 2026-08-11

## 운영 원칙

- 모든 대본·코드 변경은 먼저 테스트 또는 smoke 검증을 추가한다.
- 시간 판정의 권위는 TTS 결과의 `scene_audio_manifest.json`이다.
- 전체 음성이 완성되지 않은 상태에서는 본편 job을 완료로 표시하지 않는다.
- 실패한 음성·manifest는 삭제하지 않고 백업 폴더로 격리한다.
- 조사에서 확인한 근거와 창작적 판단을 구분한다. 커뮤니티 자료는 효과 증명이 아니라 표현 탐색용으로만 사용한다.

## 진행 기록

### 2026-08-11 — 1차 구현

- `hook_phase` 메타데이터를 `build_episode.py`, `pack_to_hermes.py`, `build_full_job.py` 전 구간에 연결했다.
- 에피소드 전체 본문 뱅크를 직접 덮어쓰지 않고 다음 오버라이드 파일을 사용하도록 변경했다.
  - `bible_healing/config/opening_hooks/ep01_anxious_night.yaml`
- 첫 훅 문장 구조를 다음 4단계로 고정했다.
  - `hook`: 불 껐지만 머릿속은 환한 밤
  - `mirror`: 오늘의 말과 내일의 걱정
  - `validate`: 약해서가 아니라 오래 버틴 결과일 수 있음
  - `permission_bridge`: 지금 당장 해결하지 않아도 되며 듣기만 해도 괜찮음
- `qa_first_minute.py`, `first_minute.py` 및 자동 테스트를 추가했다.

### 문제 1 — full job 경로가 `hook_phase`를 유실

- 원인: preview 패커와 full job 생성기가 scene meta를 서로 다르게 조립했다.
- 수정: `build_full_job.py`에 `scene_from_segment()`를 추가하고 preview와 동일한 메타데이터 계약을 적용했다.
- 검증: 첫 4개 scene에 `hook`, `mirror`, `validate`, `permission_bridge`가 보존됨.

### 문제 2 — 첫 성경 구절이 86.301초에 시작

- 원인: 훅 뒤의 기존 `u01_n0` 설명이 42.48초를 차지했고, permission bridge와 내용이 중복됐다.
- 수정: 오버라이드에서 `u01.before`를 빈 문자열로 바꾸고, 빈 before scene은 생성하지 않도록 했다.
- 결과: 첫 구절 전 불필요한 42초 설명 제거.

### 문제 3 — 운영 TTS manifest와 QA fixture의 schema 불일치

- 원인: 운영 TTS 출력은 `items`와 `duration`을 사용하고, 기존 QA는 `scenes`와 `startSeconds`만 읽었다.
- 수정: QA가 `scenes`/`items`, 명시적 시작 시각/누적 duration을 모두 지원하도록 변경했다.
- 검증: 운영형 `items` manifest 테스트 추가 및 통과.

### 문제 4 — 첫 구절이 목표보다 1.184초 빠름

- 측정: 첫 smoke에서 `43.816054초`.
- 판단: 단순 무음 삽입 대신 위안 메시지를 강화하는 것이 사용자 경험에 더 적합했다.
- 수정 문장: `지금은 무언가를 해내지 않아도 됩니다. 듣기만 해도 괜찮습니다.`
- 최종 측정: `48.552925초`.

### 문제 5 — 전체 TTS 재생성 시간 초과

- 원인: 110 scene 전체 합성은 15분 제한 안에 완료되지 않았다.
- 대응: 부분 생성 음성과 기존 오래된 manifest를 삭제하지 않고 다음 백업 폴더로 이동했다.
  - `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/audio_partial_or_stale_backup_20260811/`
- 현재 상태: full job은 새 대본·scene 구조만 준비된 상태이며, 유효한 full `scene_audio_manifest.json`은 없다.
- 따라서 full render 또는 업로드 패키지 생성은 아직 진행하지 않는다.

## 최종 smoke 검증

대상:

`bible_healing/runs/ep01_anxious_night/hermes_jobs/hook_smoke_v2/`

결과:

- scene 수: 6
- 첫 음성 시작: `0.0초`
- 첫 성경 구절 시작: `48.552925초`
- 목표 범위: `45–55초`
- hook phase: 4단계 모두 존재
- 금지 표현: 0건
- 비선택적 호흡 지시: 0건
- 테스트: `10 passed`

검증 명령:

```powershell
python bible_healing/scripts/qa_first_minute.py --job bible_healing/runs/ep01_anxious_night/hermes_jobs/hook_smoke_v2
python -m pytest bible_healing/tests -q -p no:cacheprovider
```

## 남은 작업

1. full job 전체 110 scene을 새 대본 기준으로 재합성한다.
2. full `scene_audio_manifest.json`을 새로 lock한다.
3. full manifest 기준으로 첫 구절이 다시 `45–55초`인지 재검증한다.
4. 통과 후에만 ambient plate timeline, 자막, v4 render를 진행한다.
5. 전체 길이 부족 경고(`약 74.2분 추정`)를 별도 본편 길이 문제로 해결한다. 첫 1분 훅 통과와 분리해서 관리한다.

