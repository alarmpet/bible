# 구약 힐링 영상 제작 매뉴얼

이 문서는 사람이 읽는 작업 규칙이다. 실제 렌더러는 반드시
`bible_healing/config/final_render_policy.json`을 함께 읽고,
`bible_healing/scripts/final_render_preflight.py`를 통과한 뒤에만 최종 영상을 만든다.

## 작업 전 확인 순서

1. `manual.md`, `research.md`, 관련 계획서와 현재 작업 폴더의 `scenes.json`을 확인한다.
2. 본문 화자는 `narrator`와 `scripture` 두 종류만 허용한다.
3. 성경 본문에서 괄호 설명, 곡 제목, `다윗의 시`, `영장으로 현악에 맞춘 노래`, `셀라/첼라`, 느낌표를 제거한다.
4. `할지어다`, `하리니`, `이로다`, `이니라`, `하시니라` 등 문장 종결 뒤에는 짧은 쉼을 넣는다. 단순히 마침표를 일괄 삽입하는 것이 아니라 음성 세그먼트를 분리해 실제 무음으로 만든다.
5. 남성 성경 낭독은 승인된 M4 설정만 사용한다: 속도 `0.72`, pitch shift `-8%`, 낮고 인자하며 평안한 톤.
6. 자막은 오디오 세그먼트의 실제 시작·종료 시각을 기준으로 생성한다. 장면 전체 시각을 모든 자막에 재사용하지 않는다.
7. 자막은 1080p 기준 글자 크기 72px, 한 줄 20자 이내, 최대 2줄이다. 화면을 덮는 긴 문장을 `lines[:2]`로 잘라 숨기는 방식은 금지한다.
8. 배경은 0.333 속도 기준의 느린 앰비언트 움직임을 사용한다.

## 최종 렌더 전 필수 검사

```powershell
python bible_healing/scripts/final_render_preflight.py
```

검사 실패 시 영상을 만들지 않는다. 특히 자막 싱크가 음성과 분리되거나,
성경 본문에 메타데이터가 남거나, 허용되지 않은 화자가 들어가면 중단한다.

## 반복 실수 방지

- 사람이 `manual.md`를 읽는 것만으로는 자동 적용되지 않는다. 실행 가능한 기준은 `final_render_policy.json`이며, 렌더 스크립트와 preflight가 이 정책을 기준으로 동작해야 한다.
- 새 렌더를 시작할 때 이전 MP4를 재사용하지 말고 새 작업 폴더의 오디오 manifest와 ASS를 확인한다.
- 최종 보고에는 출력 경로, 영상 길이, 화자 수, 자막 최대 줄 수, preflight 결과를 기록한다.

## 인수인계

다른 LLM이 작업할 경우에도 이 문서와 `final_render_policy.json`, preflight 결과를 먼저 확인한다.
