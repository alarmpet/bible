# 실행 로그 추가 — full render 시도

## 완료된 선행 조건

- full TTS 110 scene 완료
- full `scene_audio_manifest.json` lock 완료
- 첫 1분 QA 통과: 48.553초
- plate timeline 및 scene 110개 배정 완료
- cue 860개, SRT, ASS 생성 완료

## render 시도

실행 명령:

```powershell
python scripts\qa_healing_render.py --job runs\ep01_anxious_night\hermes_jobs\full
python scripts\render_simple_longform.py --job runs\ep01_anxious_night\hermes_jobs\full --workers 4 --skip-existing --final-name final-bible-healing-ep01-hook-v3.mp4
```

결과:

- `qa_healing_render.py`: 오류 없음. `plain_bg_report` 누락 경고가 있음.
- v3 render: 10분 실행 제한 안에 완료되지 않음.
- `final-bible-healing-ep01-hook-v3.mp4`: 생성되지 않음.
- 기존에 있던 과거 final MP4는 새 full job의 완료 증거로 사용하지 않는다.

## 현재 판정

- full audio/timing/subtitle 단계: 완료
- ambient assignment: 완료, dwell warning 6개
- final video render: 미완료
- upload package: 미착수

## 다음 작업

1. `plain_bg_report`를 생성하거나 QA 정책상 선택 경고로 명시한다.
2. scene 단위 render 결과를 batch로 생성하고, 110개가 모두 최신 wav보다 새 것인지 확인한다.
3. 별도 긴 실행 세션에서 concat을 수행한다.
4. 최종 MP4 duration, 첫 1분 frame, 자막 burn-in을 검증한다.
