# Core test & execution dependencies
pytest>=8.0.0
PyYAML>=6.0.0
requests>=2.30.0
pillow>=10.0.0
pydantic>=2.0.0
jsonschema>=4.20.0
tqdm>=4.66.0

# Optional / Specialized pipeline dependencies
# (Local TTS / audio processing / utilities)
numpy>=1.24.0
scipy>=1.10.0
edge-tts>=7.0.0
