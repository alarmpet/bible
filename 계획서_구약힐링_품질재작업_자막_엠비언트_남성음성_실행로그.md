# 품질 재작업 실행 로그

기준 계획서: `계획서_구약힐링_품질재작업_자막_엠비언트_남성음성.md`

## 2026-08-11 — 문제 원인 확인

- 자막 파일은 있으나 최종 renderer가 ASS burn-in을 실행하지 않음을 확인했다.
- `render_simple_longform.py`는 `scene_N_flow.jpg`만 사용하고 `plate_timeline.json`을 읽지 않음을 확인했다.
- scripture voice는 `M5 @ 0.85`, silence `0.35초`, 26개 scene으로 고정되어 있었다.
- 최종 영상은 새 manifest 기준 41:36.35이며, 사용자 청취 검수에서 남성 음성이 빠르고 공격적으로 들린다는 피드백을 받았다.

## 2026-08-11 — 남성 음성 A/B 샘플

추가 도구:

- `bible_healing/scripts/generate_scripture_voice_ab.py`

생성 샘플:

- `M5_slow`: M5 / speed 0.76 / silence 0.48
- `M3_slow`: M3 / speed 0.76 / silence 0.48
- `M4_slow`: M4 / speed 0.78 / silence 0.45

각 후보에 Psalm 4, Psalm 46, bridge 샘플을 생성했다.

결과 경로:

```text
bible_healing/runs/ep01_anxious_night/voice_ab/
```

처음 시스템 Python으로 실행했을 때 `supertonic` 의존성 오류가 발생했다. 기존 full TTS와 동일한 전용 `.venv-win` Python으로 재실행해 9개 샘플 생성에 성공했다.

## 다음

1. A/B 샘플 청취 후 후보를 선택한다.
2. 선택한 후보로 scripture만 새로 합성한다.
3. ASS burn-in smoke와 plate 기반 ambient smoke를 각각 만든다.
4. 두 smoke를 통합해 60초 검수본을 만든다.
