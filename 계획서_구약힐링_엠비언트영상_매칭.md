# 계획서: 구약 힐링 — 저움직임 엠비언트 영상 매칭

> **작성일:** 2026-08-11  
> **개정:** 2026-08-11 (리뷰 반영 v1.1)  
> **리뷰 근거:** `계획서_구약힐링_엠비언트영상_매칭_리뷰.md` — 코드·산출물 대조 후 **타당한 항목만** 반영  
> **전제:** 기본 보이스 = `config/voice_defaults.json` (F5 나레이션 / M5 말씀)  
> **면책·저작권 문구:** 낭독 제외, 설명란만 (`config/licenses.yaml`)

---

## 0. 한 줄 요약

| 항목 | 결정 |
|------|------|
| 비주얼 밀도 | **약 10분당 1플레이트** — **품질 목표(가설)** 이지 절대 법칙 아님 |
| 움직임 | **Still 기본** · 선택 시 플레이트 **전체 최대 ~4%** Ken Burns (분당 % 아님) |
| 매칭 단위 | **unit 연속성 + 키워드** 우선, 시간은 soft constraint |
| 시간 진실 | **`scene_audio_manifest`(lock)만** 사용. `scenes.json`은 텍스트·unit 메타 |
| 렌더 1차 | **기존 `render_healing_v3.py` 호환** — plate → `scene_N_flow.jpg` 매핑 공급 |
| 이미지 규칙 | **글자·로고·워터마크·의도하지 않은 인물 얼굴 없음** |
| 자막 | 별도 ASS 타임드 레이어 (v3 burn-in). 플레이트 계획이 자막 QA를 대체하지 않음 |

---

## 1. 리뷰 검증 결과 (채택 / 기각)

| 리뷰 지적 | 검증 | 계획 반영 |
|-----------|------|-----------|
| 저밀도 플레이트 방향 타당 | 채택 (제작비·수면형 UX 논리) | 유지 |
| `research.md` 없음 · “채널 관행” 근거 없음 | **사실** | “관행” → **UX 가설**로 강등 |
| 계획의 스크립트·렌더러 미구현 | **사실** (코드 부재 확인) | 1차는 v3 호환 경로로 명시 |
| v3는 `scene_N_flow.jpg` + `scene_N.wav` 계약 | **사실** | 매핑 후 동일 입력 공급 |
| 플레이트/씬 매핑 manifest 필요 | **타당** | `plate_timeline` + `scene_assignments` 스키마 |
| `scene_audio_manifest`가 시간 권위 | **타당** | 단일 시간 원천으로 고정 |
| clamp(8,12)가 짧은 영상에 위험 | **타당** | 구간별 규칙·경고 분리 |
| Ken Burns “%/분” 표기 오류 | **타당** (4%/분×10분=40% 줌) | **플레이트 총 줌 ≤4%** |
| crossfade vs concat 불일치 | **타당** (v3에 xfade 없음) | 초기 smoke **비필수**, 플레이트 경계만 후속 |
| 말씀 구간 중 전환 금지 우선순위 | **타당** | hard constraint 명시 |
| OCR만으로 이미지 QA 부족 | **타당** | V-게이트 확장 (수동·provenance 포함) |
| 기존 음성·자막 QA 대체 아님 | **타당** | 플레이트 QA는 **추가** 계층 |
| 12–15분 smoke 선행 | **타당** | Phase 1 필수 |
| 새 `render_ambient_v1` 즉시 작성 | **기각(우선순위)** | 호환 경로 검증 전 신규 렌더러 금지 |
| face detect 등 전부 자동화 필수 | **부분 채택** | v1은 수동+간단 검사, 자동화는 후속 |

---

## 2. “10분당 1장”의 위치 (가설)

| 방식 | 판단 |
|------|------|
| 씬마다 이미지 (100+) | 제작비·몰입 측면에서 **비권장** |
| 전편 1장 | 단조·브랜드 약함 → **비권장** |
| **~10분당 1플레이트** | **UX 가설·제작 목표** (측정 근거 문서 없음 → 가설로 운용) |
| 유닛마다 1장 (26장) | 100분 기준 다소 잦음 → 예외 구간만 |

**운영 수식 (locked duration 기준)**

```
D_min = locked_duration_sec / 60

if D_min < 60:
    target_count = clamp(ceil(D_min / 10), 6, 8)
else:
    target_count = clamp(ceil(D_min / 10), 8, 12)
```

- **목표 체류:** 약 8–12분/플레이트  
- **경고:** 단일 플레이트 **&lt; 6분** 또는 **&gt; 14분**  
- 경고는 수동 검토 트리거이지 자동 fail-closed 필수는 아님 (말씀·unit hard rule이 먼저)

---

## 3. Hard / Soft 제약 (우선순위)

리뷰 §3.2 채택. **위에서 아래 순으로 적용.**

| 순위 | 제약 | 종류 |
|------|------|------|
| 1 | **동일 성경 낭독 구간(`speaker=scripture` 연속) 내부에서 플레이트 전환 금지** | **Hard** |
| 2 | 가능하면 **동일 `unit` 내부에서 전환 금지** | Hard에 가깝게 |
| 3 | 키워드·정서 클러스터 일치 | Soft |
| 4 | ~10분 목표에 가깝게 조정 | **Soft** |
| 5 | 6–14분 체류 경고 밴드 | Soft (경고) |

즉 **시간은 soft**, **말씀·unit 연속성은 hard**.

---

## 4. 매칭 철학 (ep01 예시)

ep01「불안한 밤」유닛 묶음은 **초안 가이드**이며, TTS lock 후 시간이 재계산되면 **unit 경계를 유지한 채** 플레이트 경계만 조정한다.

| 플레이트 | 유닛 묶음(예) | 키워드 | 모티프 |
|----------|---------------|--------|--------|
| P01 | opening, u01–u03 | 밤, 잠, 숨 | 침실 창·달빛 |
| P02 | u04–u06 | 빛, 피난처 | 촛불·안전한 방 |
| P03 | u07–u09 | 지킴, 산 | 먼 산 능선 |
| P04 | u10–u12 | 평강, 상한 마음 | 잔잔한 호수 |
| P05 | u13–u15 | 갈증, 평안 | 새벽 안개 |
| P06 | u16–u18 | 저녁 기도, 잠잠 | 비 오는 창 |
| P07 | u19–u21 | 광야, 새 힘 | 별 하늘 |
| P08 | u22–u24 | 깊음, 기다림 | 숲 오솔길 |
| P09 | u25–u26 | 고난, 아침 | 여명 수평선 |
| P10 | closing | 쉼 | 고요한 방 |

### 키워드 → 모티프

| 클러스터 | 모티프 | 움직임 |
|----------|--------|--------|
| 밤·잠 | 침실 창, 달 | Still |
| 목자·물가 | 언덕·물가 (무인) | Still 또는 총 4% KB |
| 피난처 | 따뜻한 실내 | Still |
| 빛 | 촛불 | Still (불꽃 루프는 후속 옵션) |
| 산 | 먼 능선 | Still |
| 평강·물 | 호수 | Still |
| 아침 | 여명 | Still |

**금지:** 전쟁 재현, 유명인 얼굴, **이미지 속 글자/로고/워터마크**.

---

## 5. 데이터 계약 (리뷰 §2.2·§4 채택)

### 5.1 입력 vs 산출물

| 파일 | 시점 | 역할 |
|------|------|------|
| `config/ambient_plates_ep01.yaml` | 사전 | 키워드·모티프·프롬프트·허용 motion **만** (시간 없음) |
| `scene_audio_manifest.json` | TTS lock 후 | **시간·순서 단일 원천** |
| `scenes.json` | pack 시 | unit, speaker, text, ref 메타 |
| `plate_timeline.json` | **lock 이후 생성** | 플레이트 구간 + scene_assignments (결정적 산출물) |

`ambient_plates_*.yaml`에 `start_sec`를 넣지 않는다. 시간은 항상 lock 이후 스크립트가 쓴다.

### 5.2 `plate_timeline.json` 스키마 (필수 필드)

```json
{
  "schema_version": "1.0",
  "episode_id": "ep01_anxious_night",
  "audio_manifest_sha256": "<sha256 of scene_audio_manifest.json>",
  "locked_duration_sec": 3884.2,
  "target_plate_count": 10,
  "plates": [
    {
      "id": "P01",
      "start_sec": 0.0,
      "end_sec": 612.4,
      "unit_ids": ["opening", "u01", "u02", "u03"],
      "asset": "assets/generated/ep01/plates/P01.jpg",
      "motif": "bedroom_moonlight_window",
      "motion": { "type": "still", "zoom_start": 1.0, "zoom_end": 1.0 }
    }
  ],
  "scene_assignments": [
    { "order": 1, "plate_id": "P01", "start_sec": 0.0, "end_sec": 18.2 }
  ]
}
```

- 모든 scene `order`는 **정확히 하나의** `plate_id`.  
- 모든 오디오 구간은 플레이트에 **한 번씩** 커버 (누락·중복 금지).  
- `scenes.json`의 unit/speaker와 manifest 시간이 불일치하면 **fail**.

### 5.3 YAML 초안 형태 (시간 없음)

```yaml
episode_id: ep01_anxious_night
policy:
  minutes_per_plate_target: 10
  dwell_warn_min_sec: 360
  dwell_warn_max_sec: 840
  motion_default: still
  # Ken Burns: 분당이 아니라 플레이트 전체
  kenburns_zoom_total_max: 0.04
  crossfade_sec: 1.5          # 후속; smoke 비필수
  crossfade_required_in_smoke: false
  no_text_in_image: true

plates:
  - id: P01
    keywords: [밤, 잠, 숨, 불안]
    motif: bedroom_moonlight_window
    preferred_unit_ids: [opening, u01, u02, u03]
    prompt: >
      Photoreal calm bedroom at night, soft moonlight through sheer curtains,
      empty room, no people, no text, no logos, 16:9 ambient meditation still
```

---

## 6. 파이프라인 (구현 순서 확정)

```
[1] 대본 확정 (면책 낭독 없음)
[2] TTS (voice_defaults) + scripture verse expand
[3] lock → scene_audio_manifest.json          ← 시간 진실
[4] build_plate_timeline.py
      - scenes.json 에서 unit/speaker
      - manifest 에서 start/end
      - hard: scripture 연속 구간 내 전환 금지
      - soft: ~10분·키워드 클러스터
      - 출력: plate_timeline.json
[5] assign_plates_to_scenes.py (또는 timeline에 통합)
      - 각 order → plate asset 복사/하드링크 → scene_N_flow.jpg
      - 매핑 로그를 plate_timeline.scene_assignments 에 유지
[6] ensure_plate_assets.py
      - 에셋 없으면 생성, provenance 기록
[7] 기존 QA (voice / cues / ASS) 통과
[8] render_healing_v3.py                     ← 1차 렌더 (신규 렌더러 금지)
[9] qa_ambient_plates.py + 수동 시청
```

### 6.1 렌더 호환 (리뷰 핵심 채택)

| 오해 | 정정 |
|------|------|
| “v3가 플레이트를 이해한다” | **아니오.** v3는 `scene_N_flow.jpg` + wav 만 본다. |
| “복사만 하면 추적 가능” | **아니오.** `scene_assignments` manifest 필수. |
| “즉시 render_ambient_v1” | **연기.** smoke에서 v3 호환 검증 후 최적화 검토. |

### 6.2 crossfade

- **Smoke:** 필수 아님 (still + hard cut 또는 동일 이미지 연속).  
- **Full 후속:** **plate_id 경계에서만** 1.2–2.0s crossfade 검토.  
- 씬 경계마다 fade 금지 (같은 플레이트 내부).  
- 오디오 길이 변형 금지.

---

## 7. 움직임 스펙 (수정)

| 모드 | 파라미터 | 단계 |
|------|----------|------|
| **Still** | 줌 없음 | **기본 · smoke 필수 경로** |
| **Micro Ken Burns** | `zoom_end = zoom_start + ≤0.04` (플레이트 **전체**), linear | 선택 · A/B 실험 |
| **Loop video** | 4–8초 루프 (불꽃 등) | 후속 특수 모티프만 |
| 금지 | 빠른 패닝, 반복 줌, 분당 수 % 줌 | — |

**잘못된 표기 폐기:** `kenburns_percent_per_min: 4` (10분이면 40% 줌).

---

## 8. 프롬프트 템플릿

```
[motif], calm ambient atmosphere, soft low contrast,
cinematic but peaceful, empty of people, no faces,
no text, no subtitles, no logos, no watermark,
16:9, meditation youtube background
```

에셋 provenance 예:

```json
{
  "asset": "plates/P01.jpg",
  "sha256": "...",
  "source": "generated",
  "prompt": "...",
  "negative_prompt": "text, logo, watermark, face, subtitles",
  "review": { "ocr": false, "faces": false, "approved": true }
}
```

---

## 9. 구현 단계 (개정)

### Phase 0 — 계약 (0.5–1일) ✅ 2026-08-11

- [x] 면책 낭독 제거 · voice_defaults 고정  
- [x] `build_episode` 반영 (면책 없음)  
- [x] `config/ambient_plates_ep01.yaml` (시간 필드 없음)  
- [x] `plate_timeline` 스키마 문서화 (본 절 §5.2)  
- [x] `scripts/build_plate_timeline.py`  
- [x] `scripts/assign_plates_to_scenes.py`  
- [x] `scripts/qa_ambient_plates.py`  

### Phase 1 — Smoke (필수, Full 전) ✅ 최소 경로 (sample5 ~6.5분)

1. [x] sample5_F10_M10 오디오 (~6.5분, 12–15분 미만 → 플레이트 1장 정상)  
2. [x] 플레이트 **1장** (P01)  
3. [x] `plate_timeline.json` + 전 scene `plate_id=P01`  
4. [x] **`render_healing_v3.py`** → `final-sample5-ambient-smoke.mp4`  
5. [x] QA ambient V1–V5 통과 · ASS burn-in  
6. [ ] (선택) 12–15분 구간으로 2플레이트 경계 검증  
7. [ ] (선택 A/B) Still vs 총 4% Ken Burns

### Phase 2 — Full 플레이트 에셋

- 목표 개수 = §2 수식 (lock 길이 기준).  
- 16:9 · provenance · 수동 승인.  

### Phase 3 — Full 렌더 · QA

- 기존 voice/cues/ASS 게이트 **선행**.  
- 아래 V-게이트 **추가**.

---

## 10. QA 게이트 (개정)

### 10.1 기존 게이트 (플레이트와 무관, 선행)

- 보이스 = `voice_defaults`  
- 면책 문구 오디오 없음  
- ASS burn-in · 타임드 자막 정책  
- 무음 억지 패드 없음  
- lock manifest 유효  

### 10.2 플레이트 게이트 V1–V10 (리뷰 §4 채택·정리)

| ID | 게이트 |
|----|--------|
| **V1** | locked 오디오 전 구간이 플레이트에 **정확히 한 번씩** 커버 |
| **V2** | 모든 scene order에 **plate_id 정확히 1개** |
| **V3** | **말씀 낭독 중간**에 플레이트 전환 없음 |
| **V4** | 플레이트 수가 duration 목표 근처; **&lt;6분 / &gt;14분 체류 경고** |
| **V5** | 이미지 존재 · 16:9 · 최소 해상도(예: 1920×1080) |
| **V6** | OCR 본문·로고·워터마크 없음; 의도하지 않은 얼굴 없음(수동 또는 후속 자동화) |
| **V7** | 최종 MP4에 ASS 합성 · 초/중/후 샘플 자막 확인 |
| **V8** | 최종 영상 길이 ≈ 오디오 총 길이 (허용 오차 내) |
| **V9** | 에셋 provenance · prompt 보존 |
| **V10** | 수동: 플레이트 경계 밝기·색·움직임 급변 없음 |

---

## 11. 파일 맵

| 경로 | 역할 |
|------|------|
| `config/voice_defaults.json` | 기본 보이스 |
| `config/licenses.yaml` | 설명란 면책 |
| `config/ambient_plates_ep01.yaml` | 키워드·모티프·프롬프트 (무시간) |
| `scripts/build_plate_timeline.py` | lock 후 timeline 생성 |
| `scripts/assign_plates_to_scenes.py` | order→flow.jpg 공급 |
| `scripts/qa_ambient_plates.py` | V1–V10 |
| `scripts/render_healing_v3.py` | **1차 렌더 (유지)** |
| `assets/generated/ep01/plates/` | 플레이트 에셋 |
| `runs/.../plate_timeline.json` | 결정적 산출물 |

---

## 12. 리스크

| 리스크 | 대응 |
|--------|------|
| 10분 정지 지루함 | 모티프 품질 우선 · 선택적 총 4% KB · A/B smoke |
| unit 경계 ≠ 10분 | hard: 말씀/unit · soft: 시간 |
| 복사만으로 매핑 유실 | `scene_assignments` 필수 |
| crossfade가 오디오 깨뜨림 | smoke 제외 · 플레이트 경계만 · 오디오 무변형 |
| 플레이트 계획이 품질 실패 은폐 | 기존 배포 금지 사유(음성·자막) 선행 해결 |

---

## 13. 결론

- 저밀도 엠비언트 플레이트 방향은 **유지**.  
- “10분 1장”은 **가설·목표**이며, **말씀 연속·unit·lock 시간**이 우선.  
- 구현은 **manifest 계약 → v3 호환 smoke → Full** 순서.  
- Ken Burns·crossfade 수치는 리뷰에서 지적된 오류를 **수정·연기**했다.

**다음 구현 순서**

1. `ambient_plates_ep01.yaml`  
2. `build_plate_timeline.py` + `assign_plates_to_scenes.py`  
3. 1–2 플레이트 **12–15분(또는 sample5) smoke + v3**  
4. QA 스크립트  
5. Full 플레이트·본편  

---

## 부록: 리뷰 문서와의 관계

- 상세 검증 논증: `계획서_구약힐링_엠비언트영상_매칭_리뷰.md`  
- 본 개정은 리뷰의 **검증된 지적만** 규범으로 승격.  
- 미검증 주장(특정 채널 완주율 수치 등)은 포함하지 않음.

---

*문서 끝 · v1.1*
