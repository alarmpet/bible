# 구약 힐링 영상 문제 정밀 진단 및 재작업 계획

작성일: 2026-08-11
검수 기준 이미지: `C:\Users\amd\Downloads\캡처1.PNG`
대상 영상: `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/final-bible-healing-ep01-hook-v3.mp4`

## 1. 사용자가 원하는 결과의 정확한 정의

검수 이미지의 핵심은 촛불 사진 자체가 아니다. 한 장면이 다음처럼 아주 느리게 살아 있는 상태다.

- 촛불 flame이 미세하게 흔들린다.
- 촛불 주변의 밝기와 벽면 glow가 아주 약하게 호흡한다.
- 뒤쪽 bokeh와 작은 빛 점이 불규칙하지만 과하지 않게 반짝인다.
- 카메라가 거의 느끼기 어려운 정도로 천천히 push-in 또는 drift한다.
- 전체 장면은 조용하고 안정적이며, flashy한 전환·빠른 zoom·강한 particle은 금지한다.
- 1장의 이미지가 계속 유지되더라도 “살아 있는 밤”처럼 보여야 한다.

따라서 목표 명칭은 `ambient slideshow`가 아니라 `micro-motion candle ambient`로 정의한다.

## 2. 문제별 원인 진단

### A. 자막이 없거나 싱크가 맞지 않음

두 가지 문제가 겹쳐 있다.

#### A-1. 최종 영상에 자막 burn-in이 빠짐

`subtitles-timed-ko.srt`와 `subtitles-timed-ko.ass`는 생성됐지만, 최종 full 영상은 `render_simple_longform.py`로 생성됐다. 이 renderer는 SRT/ASS를 영상에 태우지 않는다.

`render_healing_v3.py`에는 ASS burn-in 경로가 있으나 full final 생성에서 사용하지 않았다.

#### A-2. 현재 자막 타이밍은 실제 발화 타이밍이 아님

`build_cues_from_manifest.py`는 각 scene의 전체 시작·종료 시간 안에서 문장을 글자 수 기준으로 균등 분할한다. 예를 들어 한 문장을 2개 cue로 나누면 실제 음성의 단어 경계나 호흡을 분석하지 않고 시간만 나눈다.

따라서 자막 파일이 화면에 보이더라도 다음 문제가 생길 수 있다.

- 말하기 전에 다음 자막이 나타남
- 이미 말한 문장이 화면에 남음
- 성경 구절의 쉼표·절 단위와 화면 전환이 어긋남
- TTS chunk 경계와 자막 경계가 다름

판정: 자막 문제는 `파일 없음`이 아니라 `burn-in 누락 + 균등분할 기반의 부정확한 cue` 문제다.

### B. ambient가 아닌 정지 이미지 영상

현재 `render_simple_longform.py`는 다음만 입력으로 사용한다.

```text
scene_N_flow.jpg + scene_N.wav
```

`plate_timeline.json`에 plate 배정 정보가 있어도 renderer가 이를 읽지 않는다. 또 `scene_N_flow.jpg`는 plate 이미지의 정지 복사본이므로 촛불 flame, glow, bokeh 움직임이 없다.

판정: 현재 결과는 “ambient asset을 사용한 정지 slideshow”이며, 사용자가 요구한 micro-motion ambient가 아니다.

### C. 남성 음성이 목회자처럼 평안하지 않고 화난 듯함

현재 full 설정:

```text
voice: M5
speed: 0.85
total_step: 16
silence_duration: 0.35
```

M5의 speed 숫자만 낮추는 것으로는 해결되지 않는다. 실제 요구는 다음 음성 특성이다.

- 저음이지만 어둡고 위협적이지 않음
- 말끝이 내려앉고, 문장 사이에 기다림이 있음
- 강세가 평평하고 부드러움
- 설교하듯 몰아붙이지 않고, 곁에서 읽어주는 느낌
- 자음 어택과 음량 변화가 작음

현재 M3 slow smoke는 생성·첫 1분 QA까지는 통과했지만, 사용자 청취 승인 전이므로 최종 후보로 확정하지 않는다. 샘플의 실제 청취 평가가 필요하다.

## 3. 수정 방향

### 3.1 자막

새 자막 pipeline을 다음처럼 변경한다.

```text
TTS chunk/scene timing
→ phrase cue 생성
→ ASS 생성
→ concat 후 ASS burn-in
→ frame 검수 + audio/cue drift QA
```

최소 1차 수정:

- 최종 renderer에서 반드시 ASS burn-in
- scene 전체 균등분할 대신 문장부호·TTS chunk 단위 우선 분할
- 한 cue 최소 0.8초, 최대 4.5초
- cue 시작·종료가 이전/다음 cue와 겹치지 않음
- 성경 구절은 절 또는 호흡 단위로 분할

정밀 2차 수정:

- TTS chunk별 실제 wav duration을 manifest에 기록
- text chunk와 audio chunk를 1:1로 매핑
- 필요하면 forced alignment를 추가해 단어/구 단위 cue 생성

### 3.2 촛불 ambient

각 plate asset을 영상 프레임으로 변환할 때 다음 effect layer를 적용한다.

1. **Flame layer**: 촛불 영역에 매우 작은 밝기·세로 scale·shape warp 변화
2. **Glow layer**: flame 주변 radial blur/opacity를 비주기적으로 변화
3. **Bokeh layer**: 뒤쪽 빛 점의 opacity와 blur를 서로 다른 주기로 변화
4. **Camera layer**: 6–12분 동안 1–2% 이하의 느린 push-in 또는 lateral drift
5. **Grain layer**: 필요할 때만 아주 약한 film grain

중요한 원칙:

- 원본 촛불의 위치와 형태를 바꾸지 않는다.
- flame이 만화처럼 펄럭이지 않게 한다.
- 모든 pixel을 흔드는 shake는 사용하지 않는다.
- 주기성이 눈에 띄지 않도록 seed와 cycle length를 장면별로 바꾼다.
- plate 전환은 1.5–3초 crossfade를 적용한다.

구현 우선순위:

- 1차: ffmpeg 기반 `zoompan + subtle brightness + glow overlay`
- 2차: 촛불 mask를 사용한 flame/glow 전용 motion
- 3차: bokeh particle layer와 장시간 seamless loop

### 3.3 목회자형 남성 음성

현재 M5 full audio는 폐기하지 않고 보관하되 최종본으로 사용하지 않는다.

후보 설정:

| 후보 | voice | speed | total_step | silence | 목적 |
|---|---|---:|---:|---:|---|
| A | M3 | 0.72–0.76 | 20–24 | 0.48–0.60 | 부드럽고 낮은 목소리 우선 |
| B | M4 | 0.74–0.78 | 20–24 | 0.45–0.55 | 따뜻함과 명료도 균형 |
| C | M5 | 0.70–0.74 | 24 | 0.55–0.65 | 기존 음색을 최대한 낮춤 |

음성 후보는 반드시 같은 Psalm 4, Psalm 46, bridge로 비교한다. 선택 기준은 숫자 duration이 아니라 다음 청취 인상이다.

- 화난 억양이 없는가
- 명령조가 아닌가
- 낮지만 답답하지 않은가
- 문장 끝이 안정적으로 내려오는가
- 2분 이상 들어도 긴장감이 올라가지 않는가

후보 선정 전에는 26개 scripture 전체를 다시 만들지 않는다.

## 4. 검증용 smoke 설계

### Smoke A — 자막

- 2분 이내
- ASS burn-in 포함
- 첫 hook 4단계와 Psalm 4 일부
- frame 0, 30, 60, 90초에서 자막 확인

통과 조건: 자막이 실제 영상에 보이고, 발화보다 0.2초 이상 앞서거나 0.4초 이상 늦지 않는다.

### Smoke B — 촛불 ambient

- 동일 촛불 이미지 60–120초
- flame/glow/bokeh motion 적용
- 정지 frame과 motion frame 차이 측정

통과 조건:

- 영상 초반·중반·후반 frame hash가 모두 다름
- 평균 pixel 변화는 작지만 flame/glow 영역은 변화함
- 화면 전체 shake나 과도한 flicker 없음

### Smoke C — 통합

- 선택 voice
- ASS burn-in
- 촛불 micro-motion
- 첫 구절 45–55초

세 smoke를 모두 통과한 후에만 full 재생성한다.

## 5. 완료 조건

- [ ] 촛불 flame이 미세하게 살아 있음
- [ ] 주변 glow와 bokeh가 느리게 변함
- [ ] 카메라 movement가 거의 느껴질 정도로만 존재
- [ ] 자막이 실제 최종 영상에 burn-in됨
- [ ] 자막이 실제 발화와 동기화됨
- [ ] 남성 목소리가 낮고 인자하며 안정적임
- [ ] 공격적 강세·빠른 호흡이 없음
- [ ] 첫 구절 45–55초 유지
- [ ] full 영상 재생 후에도 subtitle/ambient/audio drift 없음

## 6. 관련 산출물

- 기존 문제 영상: `bible_healing/runs/ep01_anxious_night/hermes_jobs/full/final-bible-healing-ep01-hook-v3.mp4`
- 자막 burn-in smoke: `bible_healing/runs/ep01_anxious_night/hermes_jobs/voice_smoke_M3/final-voice-M3-caption-smoke.mp4`
- 남성 음성 후보: `bible_healing/runs/ep01_anxious_night/voice_ab/`
- 새 재작업 계획: 본 문서

