## 전체 배포영상 배경·자막 최종 규칙

- 전체 배포영상은 `bible_healing/assets/movie-sample/pingpong-1min`의 모든 1분 앰비언트 MP4를 순환 사용한다.
- 단일 촛불 영상만 반복하지 않는다.
- 배경 움직임은 `setpts=3*PTS`, 즉 0.333배속으로 느리게 재생한다.
- 전체 배포영상에는 `subtitles-timed-ko.ass`를 반드시 번인한다.
- 챕터 표시만 있고 본문 자막이 없는 파일은 배포본으로 인정하지 않는다.
