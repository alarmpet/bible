# 리뷰: 자막 2줄·성경 낭독 호흡 · 최종영상 실수 방지 계획

> **작성일:** 2026-08-12  
> **대상 문서:** `C:\Users\amd\module\계획서_구약힐링_자막과호흡_최종영상_실수방지.md`  
> **대조 자료:**  
> - `C:\Users\amd\module\research.md`  
> - `bible_healing/` 코드·설정·full job 산출  
> - Hermes 자막 규약 (`capcut-single-line-caption`, UI presets)  
> - 기존 계획: 엠비언트 매칭, Hermes 업그레이드, 첫 1분 훅  

---

## 1. 총평

대상 문서는 **실무에서 이미 터진 사고**(한 자막에 구절 전문 몰아넣기, 문장부호 없는 개역한글 연속 낭독)를 정확히 짚고, **렌더 전 게이트**로 묶으려는 방향이 타당하다.

다만 세 가지를 반드시 구분해야 한다.

| 층 | 내용 | 대상 문서 | 코드 현실 |
|----|------|-----------|-----------|
| A. 문제 인식 | 5줄 자막, 무호흡 낭독 | 정확 | full 산출에서도 부분 재현 가능 |
| B. 규칙 제안 | 2줄·18–24자·종결어 pause·표제 제거 | 대체로 타당 | **부분만 구현**, 수치 불일치 |
| C. 워크플로우 완성 정의 | 10단계 순서·QA 실패 조건 | 좋음 | **자동화·강제 게이트 미연결** |

**한 줄 판정:**  
문서 채택 가능. 단 **그대로 “완료 기준”으로 쓰면 코드와 충돌**하고, **research.md 범위(첫 1분 훅)와 주제가 겹치지만 연결이 없다.** 아래 수정을 반영한 뒤 규범 문서로 승격할 것을 권한다.

---

## 2. research.md와의 관계 (검증)

`research.md`는 **첫 1분 훅·유지율·공감 언어·루프 영상 기술** 조사다.  
대상 문서의 **자막 줄 수·종결어 pause**와는 직접 겹치지 않는다.

| research.md 결론 | 자막·호흡 문서와의 시너지 | 반영 권고 |
|------------------|---------------------------|-----------|
| Intro = 첫 30초 잔존 비율 (YouTube) | 자막이 화면을 가리면 첫 30초 이탈↑ | 훅 구간 자막은 **더 짧게·더 크게**, 2줄 엄수 |
| 구체 공감이 설명보다 먼저 | 오프닝 자막도 설명문 장문 금지 | 훅 대본 + 자막 규칙 **동시** 적용 |
| 첫 말씀 45–55초 목표 | 말씀 구간 자막·호흡 품질이 더 일찍 노출 | 말씀 TTS 전처리 우선순위↑ |
| 호흡은 선택형 1회 | TTS pause와 혼동 금지 | “대본 호흡 안내” ≠ “종결어 pause” 분리 명시 |
| seamless loop 주의 | 엠비언트와 별 이슈 | 본 문서 범위 밖으로 링크만 |

**의견:** 실수방지 문서에 `research.md`를 “반드시 읽으라”고만 적지 말고,  
**어느 절이 어느 규칙에 연결되는지** 2–3줄로 매핑하는 편이 인수인계에 유용하다.  
`manual.md`는 워크스페이스에 **존재하지 않음** — 대상 문서 §인수인계의 `manual.md` 참조는 **삭제 또는 경로 정정** 필요.

---

## 3. 코드베이스·워크플로우 대조

### 3.1 현재 파이프 (실제)

```
build_episode / build_full_job
  → scripture_tts_prep (표제·셀라 제거, !→., 절 단위 분할)
  → tts_multi_voice (SuperTonic, max_chunk/verse units)
  → lock_audio_manifest          ← 시간 진실
  → build_cues_from_manifest     ← Hermes식 단일 행 분할
  → build_ass_from_cues
  → (plate assign)
  → render_healing_v3 / burn_final_ass
  → qa_healing_render (부분)
```

대상 문서 10단계는 **방향은 맞으나**, 아래가 **코드에 없거나 느슨**하다.

| 문서 요구 | 코드 상태 | 갭 |
|-----------|-----------|-----|
| 원문 보존본 / TTS 정제본 분리 | `text_display` 필드 일부, 일관 스키마·파일 저장 **약함** | `text_display_source` vs `text_tts` 계약 파일화 필요 |
| 종결어 뒤 마침표·pause 350–550ms | **미구현** (`!` 완화 + 절 분할만) | 핵심 미구현 |
| 절 사이 550–800ms | 대략 **0.22s** gap (`tts_multi_voice`) | 수치 미달 |
| 쉼표 180–280ms | **미구현** (엔진 내부 운율에 의존) | SuperTonic에 명시 pause 토큰 없으면 근사만 가능 |
| 2줄 자막 | policy에 `maxLines: 2` 있으나 split은 **사실상 1줄 phrase** | “2줄” vs “1줄 큐 연속” 정의 필요 |
| 18–20자 권장 / 24 상한 | **상한 24만 강제**, 권장 18–20 **미강제** | soft target 도입 가능 |
| 폰트 108 / 112px | 실제 **96 / 100px** | 문서·코드 불일치 |
| 3줄 이상 QA fail | `qa_healing_render`는 **max chars만**, 줄 수·안전영역·셀라 **미검** | QA 확장 필수 |
| 2초 무호흡 fail | **미구현** | 정의·측정 방법 필요 |
| ASS는 정책 파일에서만 생성 | `build_ass_from_cues` ← policy | 대체로 충족 |
| TTS 전 scenes.json 직행 금지 | full 경로에 expand 있으나 **강제 게이트 없음** | preflight 스크립트 권장 |

### 3.2 full job 실측 (2026-08-12 기준)

| 항목 | 값 | 문서 대비 |
|------|-----|-----------|
| cues max_len | **24** | 상한은 통과, 권장 18–20 초과 큐 다수 |
| cue 모드 | 한 이벤트 = **한 줄 phrase** | “2줄” 화면 구성과 다름 |
| scenes에 셀라/표제 | 일부 **잔존** (`has_selah` True 가능) | narration 필드 vs TTS segment 이중성 |
| 최종 영상 길이 | ~50분 (rest 제거 후) | 문서 범위 외이나 품질 게이트와 무관하지 않음 |

### 3.3 Hermes 규약과의 정합

| Hermes | 문서 | 의견 |
|--------|------|------|
| clean-news **12자·2줄** | 18–20·2줄·상한 24 | 가로 1080p에서 12는 짧다는 **사용자 피드백**이 이미 있음 → 24 상한은 타당 |
| CapCut RA **16자 단일행** | 2줄 허용 | 롱폼 힐링은 **1줄 큰 글씨** 또는 **2줄 묶음** 선택지 명시 필요 |
| minDisplay **0.65s** | 명시 약함 | 문서에 **최소 표시 시간** 추가 권장 (policy 0.75s와 정합) |
| Whisper word timestamps | 문서에 없음 | “추정 글자 수만으로 확정 금지”와 맞으려면 **lock duration + (선택) Whisper**를 순서에 명시 |

---

## 4. 문서 조항별 타당성

### 4.1 필수 자막 규칙 — **대체로 채택**

| 조항 | 판정 | 코멘트 |
|------|------|--------|
| 최대 2줄 | **채택 (정의 보완)** | “한 ASS 이벤트 = 최대 2줄” vs “연속 1줄 큐”를 코드 계약으로 고정 |
| 줄당 18–20 권장, 24 상한 | **채택** | 권장선 soft, 24 hard. 현재 코드는 hard만 있음 |
| 긴 절 전체 한 이벤트 금지 | **강하게 채택** | 사고 재발 방지의 핵심 |
| 의미 단위 분할 | **채택** | 공백 분할만으로는 한국어 부족 → 종결어·쉼표 병행 |
| 글꼴 축소로 욱여넣기 금지 | **채택** | 현재 96–100px 고정 철학과 일치; 문서 108/112는 **재조정 권고** |
| 하단 안전영역 | **채택** | marginV·safe area를 policy 숫자로 고정 |
| 렌더 전 QA | **채택** | 지금 `qa_healing_render`로는 불충분 |

**폰트 크기 의견:**  
108/112px는 24자 2줄이면 **세로 점유 과다** 위험이 있다.  
실측 가능 범위에서 **본문 96–104px, 말씀 100–108px**를 권장 밴드로 두고, “줄 수 초과 시 크기 축소 금지 → **분할 강제**”가 문서 취지와 더 맞다.

### 4.2 성경 TTS 호흡 — **방향 채택, 구현 조건 추가**

| 조항 | 판정 | 코멘트 |
|------|------|--------|
| 종결어 뒤 TTS용 `.` 삽입 | **채택** | `scripture_tts_prep.soften_for_speech` 확장으로 구현 가능 |
| 화면 원문과 TTS 분리 | **강하게 채택** | 이미 부분 존재; **필드명·저장 경로 표준화** 필요 |
| pause 350–550 / 550–800 / 쉼표 180–280ms | **채택(실험값)** | SuperTonic이 SSML pause를 지원하지 않으면 **절·구 단위 합성 + silence_duration/concat gap**으로 근사. “ms 정확 보장”은 엔진 한계 명시 |
| 2초 무호흡 fail | **조건부 채택** | 측정: 한 TTS 유닛 duration / char 또는 energy 기반. **구현 전 정의 필요** |

**더 나은 방향:**  
종결어 리스트에 개역한글 빈도를 반영해 확장:

```text
할지라, 하도다, 하였도다, 하시도다, 하리로다, 이로다,
이니라, 하니라, 아니라, 느니라, 리라 (오탐 주의 — 문맥/어미 경계만)
```

오탐(본문 중간 “하리니” 아닌 경우)을 줄이려면 **어미 경계 정규식** (`…할지어다(?:\s|$)`)을 쓰고, 단위 테스트 필수.

### 4.3 표제·지시문 제거 — **채택**

이미 `strip_headers_and_selah`가 있으나, **scenes.narration / 자막 소스**에 원문이 남으면 화면·로그에 재유입된다.

**권고 계약:**

```text
segment.speaker == scripture 일 때
  text_tts        ← 낭독 전용
  text_caption    ← 화면 전용 (표제·셀라 없음, 원 문장부호 유지 가능)
  text_source     ← 보존·감사용 (표제 포함 가능)
```

TTS·cue·ASS는 **text_tts / text_caption만** 사용. QA는 세 필드 교차 검사.

### 4.4 최종 렌더 전 순서 — **채택 + 보완**

문서 1–10 순서에 다음을 **끼워 넣는 것**을 권장.

| 단계 | 추가 |
|------|------|
| 0 | `voice_defaults` / policy 해시 고정 |
| 3.5 | scripture expand **완료 여부** preflight (미완료 시 TTS 거부) |
| 5.5 | `scene_audio_manifest` lock **sha256** 기록 |
| 7.5 | plate_timeline assign (해당 시) — 자막과 독립이지만 최종 영상 전 |
| 9.5 | 최종 파일 duration ≈ lock total (±0.5s) |
| 10 | 10초·1분·3분 **프레임 스냅샷 자동 저장** (수동만 믿지 않기) |

### 4.5 절대 금지 · QA 실패 — **채택, 자동화 목록 정리**

문서 금지는 모두 타당.  
다만 “2초 무호흡”, “안전영역 초과”는 **측정 방법 없이 fail 조건을 쓰면** 구현자가 우회하거나 무시한다.

권장 QA 패키지 ID:

| ID | 검사 | 현재 |
|----|------|------|
| C1 | cue 라인 수 ≤ 2 | 미흡 |
| C2 | 라인 글자 ≤ 24 hard | 부분 |
| C3 | 라인 글자 > 20 soft warn | 없음 |
| C4 | 셀라/표제 패턴 금칙 | 없음 |
| C5 | scripture 유닛 최대 길이·종결어 pause 존재 | 없음 |
| C6 | ASS = policy 생성물 (font/size/margin) | 부분 |
| C7 | final duration vs lock | 부분 |
| C8 | 샘플 프레임 OCR “자막 줄 수” 수동 또는 휴리스틱 | 없음 |

---

## 5. 내 쪽 개선 의견 (문서에 반영 권고)

### 5.1 “2줄”을 구현 가능한 두 모드로 쪼개라

| 모드 | 정의 | 용도 |
|------|------|------|
| **M1 단일행 큐** | 이벤트 1개 = 1줄 ≤20(권장)/24(상한), 시간순 연속 | 현재 Hermes 포트와 가장 잘 맞음 |
| **M2 이중행 큐** | 이벤트 1개 = 최대 2줄, 각 줄 ≤20/24, 합쳐도 의미 단위 | “한 호흡 두 줄” 연출 |

문서가 “최대 2줄”만 쓰면 M1/M2가 섞여 **QA가 애매**해진다.  
**기본 권장: M1 (큰 글씨 1줄)** + 필요 시 M2.  
사용자 피드백(가로·큰 글씨)과도 M1이 잘 맞는다.

### 5.2 pause는 “문자 삽입”과 “무음 클립”을 분리하라

- **문자 `.` 삽입** → 엔진 운율 유도 (저비용, 불완전)  
- **절·구 단위 합성 + concat silence** → 시간 통제 (현재 절 분할과 정합)  
- SSML/break → SuperTonic 미지원 시 **약속하지 말 것**

문서의 ms 수치는 **목표 밴드**로 두고, 구현 노트에 “엔진 한계 시 verse gap으로 근사”를 명시.

### 5.3 첫 1분 훅 문서와 충돌 없게

`research.md` / 첫 1분 훅 계획: 오프닝은 **짧고 구체적**.  
자막 실수방지가 “의미 단위 분할”만 강조하면, 훅 문장까지 과분할되어 **리듬이 잘게 부서질** 수 있다.

**제안:**  
- `speaker=narrator` + `unit=opening|hook` → 분할 상한을 조금 느슨 (예: 22–24자, 최소 표시 0.9s)  
- `speaker=scripture` → 문서 규칙 **최엄격**

### 5.4 “완료 보고 금지” 문구 강화

인수인계 메모는 좋으나 강제력이 없다.

```text
quality_gate.json 에
  captions.ok && scripture_tts.ok && final_duration.ok
가 모두 true 일 때만
  HANDOFF / 업로드 패키지에 "배포 가능" 기록 허용
```

### 5.5 문서에 넣지 말아야 할 과한 약속

- 특정 ms pause의 **청각 완전 재현** (엔진 종속)  
- OCR로 “화면 가림” 자동 판정 100%  
- manual.md 등 **없는 파일** 참조  

---

## 6. research.md · 기존 계획과의 정합 맵

| 주제 | 우선 문서 | 본 실수방지 문서 역할 |
|------|-----------|------------------------|
| 첫 30–60초 훅 | research + 첫1분훅 계획 | 훅 구간 자막 예외(느슨) 명시만 |
| 엠비언트 루프 vs 스틸 | 첫1분훅·엠비언트 계획 | **범위 외** (혼동 금지) |
| 자막 줄·폰트·QA | **본 문서** | 규범 |
| 말씀 TTS 청크·셀라 | 본 문서 + scripture_tts_prep | 규범 + 구현 스펙 |
| 플레이트 10분 | 엠비언트 매칭 계획 | 독립 |

---

## 7. 권장 개정 체크리스트 (원 문서에 넣을 문장)

1. **범위:** 자막·성경 TTS 호흡·렌더 게이트. 훅/엠비언트는 링크만.  
2. **참조 수정:** `research.md` 연결 표; `manual.md` 삭제 또는 실제 경로.  
3. **모드:** 기본 자막 = **단일행 큐 M1**; 2줄은 옵션 M2.  
4. **숫자:** 권장 18–20 / hard 24; 폰트 **96–104 / 100–108** 밴드로 조정하거나 108/112 채택 시 **2줄×24자 시안 스모크 필수**.  
5. **pause:** 목표 ms + “verse/phrase concat silence로 근사”.  
6. **스키마:** `text_source` / `text_tts` / `text_caption`.  
7. **게이트:** C1–C8 자동화; 미통과 시 final 경로 생성 스크립트 exit≠0.  
8. **preflight:** `scripture_tts_prep` 미실행 job에 TTS 금지.  
9. **샘플 검증:** 10s/1m/3m 프레임 자동 export 경로 지정.  

---

## 8. 최종 권고

| 항목 | 권고 |
|------|------|
| 문서 방향 | **채택** |
| 즉시 규범화 | 문제 정의·금지 목록·display/TTS 분리·표제 제거 |
| 수정 후 규범화 | 2줄 정의, 폰트 px, pause 구현 방식, QA 자동화 |
| 보류 | ms pause 절대 보장, OCR 안전영역 자동 fail |
| 구현 우선순위 | (1) text 삼중 스키마 + 종결어 TTS 전처리 (2) cue 2줄/24자 QA 강화 (3) preflight+final gate (4) 훅 구간 예외 |

**코드 측 현재 가장 위험한 잔존 리스크**

1. 자막이 **한 줄 24자 큐**로 나가도, ASS·렌더 경로나 **구 narration 필드**가 긴 문자열을 다시 그리면 5줄 사고 재발.  
2. 종결어 pause **미구현** → “할지어다” 연속 화난 톤 재발.  
3. **완료 보고 게이트 없음** → 깨진 final(예: moov 없는 중간 파일)도 “완료”로 오인 가능.  

이 세 가지는 대상 문서 정신에 부합하며, **문서보다 코드 게이트로 먼저 막는 것**을 권한다.

---

## 9. 본 리뷰 문서 경로

```text
C:\Users\amd\module\계획서_구약힐링_자막과호흡_최종영상_실수방지_리뷰.md
```

**대상 문서 경로**

```text
C:\Users\amd\module\계획서_구약힐링_자막과호흡_최종영상_실수방지.md
```

**조사 노트 경로**

```text
C:\Users\amd\module\research.md
```

---

*리뷰 끝.*
